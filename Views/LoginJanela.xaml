<Page x:Class="GerenciadorDeSenhas.Views.LoginJanela"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GerenciadorDeSenhas.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Login">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="4*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>
        

        <TextBlock Text="Login" FontSize="34" FontWeight="Bold" VerticalAlignment="Bottom"  HorizontalAlignment="Center"/>

        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <Label Content="Usuário:" FontWeight="Bold"/>
            <TextBox Name="UsernameTextBox" Margin="0,0,0,10"/>

            <Label Content="Senha:" />
            <PasswordBox Name="PasswordBox" Margin="0,0,0,20"/>

            <Button Content="Entrar" Width="150" HorizontalAlignment="Center" Click="btnLogin"/>

            <TextBlock Text="CRIAR NOVO USUÁRIO" HorizontalAlignment="Right" Margin="0,10,0,0" Foreground="Blue" TextDecorations="Underline" MouseDown="lnkRegistrar"/>
        </StackPanel>
    </Grid>
</Page>
